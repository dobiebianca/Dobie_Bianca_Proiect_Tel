@page
@model Dobie_Bianca_Proiect_Tel.Pages.Phones.CreateModel

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Phone</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Phone.Name" class="control-label"></label>
                <input asp-for="Phone.Name" class="form-control" />
                <span asp-validation-for="Phone.Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Phone.Brand" class="control-label"></label>
                <input asp-for="Phone.Brand" class="form-control" />
                <span asp-validation-for="Phone.Brand" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Phone.Price" class="control-label"></label>
                <input asp-for="Phone.Price" class="form-control" />
                <span asp-validation-for="Phone.Price" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Phone.PublishingDate" class="control-label"></label>
                <input asp-for="Phone.PublishingDate" class="form-control" />
                <span asp-validation-for="Phone.PublishingDate" class="textdanger"></span>

                <label asp-for="Phone.SellerID" class="control-label">Seller Name</label>
                <select asp-for="Phone.SellerID" class="form-control" asp-items="ViewBag.SellerID">
                </select>
                <span asp-validation-for="Phone.SellerID" class="text-danger"></span>
            </div>  
            <div class="form-group">
                <div class="table">
                    <table>
                        <tr>
                            @{
                                int cnt = 0;
                                foreach (var cat in Model.AssignedCategoryDataList)
                                {
                                    if (cnt++ % 3 == 0)
                                    {
                                    @:</tr><tr>
                                    }
                                    @:<td>
                                        <input type="checkbox"
                                               name="selectedCourses"
                                               value="@cat.CategoryID"
                                               @(Html.Raw(cat.Assigned ? "checked=\"checked\"" : "")) />
                                        @cat.CategoryID @: @cat.Name
                                    @:</td>
                                }
                            @:</tr>
                            }
                        </table>
                    </div>
                </div>
                
                <div class="form-group">
                    <input type="submit" value="Create" class="btn btn-primary" />
                </div>
            
        </form>
    </div>
</div>

<div>
    <a asp-page="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
